/**
 * DEPRECATED — Speech-to-text moved to OpenRouter (Groq Whisper).
 * See lib/stt/openrouter-stt.ts. This file is retained only because the build
 * sandbox could not delete it; nothing imports it. Safe to delete from the repo.
 */
export * from './openrouter-stt';
